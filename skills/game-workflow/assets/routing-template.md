# Routing decision

Rules: populate every applicable field; use not-applicable with a reason for exclusions. Reuse existing record identity rather than duplicating it. Unknown intent does not authorize writes. Preserve the user's original wording by quotation or source locator.

- Record ID / revision:
- Bootstrap status: new | resumed | dispatched-bypass | not-found | needs-reconciliation
- Request source and original goal:
- Goal revision / changed intent since prior revision:
- Intent: consultation | specification | review | execution
- Flow: development | improvement | bugfix | not-applicable (reason)
- Selection reason:
- Scope / exclusions / existing authority:
- Target references:
- Resolved language preferences or profile reference (inferred fallback if absent):
- Existing run ID or reason for new work:
- Registry locator / candidate count / match basis:
- Context marker: supervised | standalone | dispatched
- Supervisor required: yes | no (bounded reason)
- Initial capability tier / selection factors: highest-judgment | complex-execution | balanced-execution | high-volume | host-default (bounded reason)
- Known decisions / proposals / material unknowns:
- Clarification owner/status: router | supervisor | not-applicable; clear | needs-clarification
- Next skill's resolved path, or direct answer:
- Handoff status and blocker, if any:

Before handoff: verify the selected skill exists, the scope matches the intent, no duplicate run is created, and supervised work has one overall supervisor. A routing record is not approval or a task plan.
